#include<iostream>
using namespace std;
int main(){
    //cout<<5%2;(output 1 aayega kyunki 5 ko 2 se divide karne par remainder 1 aata hai)

    //int a=10;
    //int b=4;
    //cout<<a%b;//output 2 aayega kyunki 10 ko 4 se divide karne par remainder 2 aata hai

    //cout<<100%4;//output 0 aayega kyunki 100 ko 4 se divide karne par remainder 0 aata hai

    //cout<<-5%2;//output -1 aayega kyunki -5 ko 2 se divide karne par remainder -1 aata hai

    /* some points to remember about modulus operator
    1. a%b = r (where r is remainder)
    2. if a<b then a%b = a
    3. if a is negative then a%b = -(abs(a)%b)
    cout<<2%40;//output 2
    [if a<b then a%b = a]
    */

    //cout<<5%-2;//[  a%(-b) = a%b]
    //cout<<-99%4;//[ (-a)%b = -(a%b)]

}